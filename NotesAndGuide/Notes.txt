1. #include<stdio.h> This is also known as Pre Processor Directive. Pre processtor directive brings the build-in functions like printf and others functions.

2. Variable is memory location name.

3. %d or %f or etc is called format specifier.

4. If a is integer and its value is 9 b is also integer and its value is 2 and if u divide them the answer will come 4 which is wrong it shoud be 4.5. This happens because int float -> float , float int -> float , int int -> int.

5. Operator precedence. 1st priority is / * % , 2nd is + - and 3rd is = , unless you use parenthesis. and if you use / * % things at the same time then it use associativity means it will run left to right. For example x*y/z will be (x*y)/z and x/y*z will be (x/y)*z.


6. Given equation 3*x/y - z+k
 3*2/3 - 3+1
 6/3 - 4 ====> this is wrong to add 3+1, because there is no bracket or something that indicates to solve the 3+1 first, so we wont do that, instead
 2 - 4
 -2
 Correct Method:
 3*2/3 - 3+1
 2 - 3 + 1 =========> always left to right
 -1 + 1
 0 =======> correct answer

 7. if you using int and y
