{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run C (Linux)",
            "type": "shell",
            "command": "gcc \"${file}\" -o \"${fileDirname}/${fileBasenameNoExtension}\" -lm && \"${fileDirname}/${fileBasenameNoExtension}\"",
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}